apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: oip-auth-ingress
  annotations:
    kubernetes.io/ingress.class: "nginx"
    cert-manager.io/cluster-issuer: "letsencrypt-staging"
spec:
  tls:
  - hosts:
    - oip-auth.k8s.jaagalabs.com
    secretName: oip-auth-tls
  rules:  
  - host: oip-auth.k8s.jaagalabs.com
    http:
      paths:
      - backend:
          serviceName: oip-auth
          servicePort: 80